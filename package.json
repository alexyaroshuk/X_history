{
  "name": "x-history",
  "version": "1.1.0",
  "description": "Chrome extension for tracking X post history",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "watch": "tsc --watch",
    "copy-assets": "echo Assets already in place",
    "clean": "rm -rf dist",
    "dev": "npm run build && npm run watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.3"
  },
  "author": "",
  "license": "MIT"
}